<Window x:Class="WinTakMeshtasticPlugin.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Meshtastic Settings"
        Width="400" Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#2D2D30"
        Foreground="#F1F1F1">

    <Window.Resources>
        <!-- Dark theme colors -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#2D2D30"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="#F1F1F1"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3F3F46"/>
        <SolidColorBrush x:Key="TextBoxBackgroundBrush" Color="#3E3E42"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#007ACC"/>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource TextBoxBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="CaretBrush" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        </Style>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#606060"/>
                            </Trigger>
                            <Trigger Property="IsDefault" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GroupBox Style -->
        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,1,1,0"
                                    Background="{StaticResource TextBoxBackgroundBrush}" CornerRadius="2,2,0,0">
                                <ContentPresenter ContentSource="Header" Margin="10,6"/>
                            </Border>
                            <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                    Background="{StaticResource BackgroundBrush}" CornerRadius="0,0,2,2">
                                <ContentPresenter Margin="12"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Settings Content -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Connection Settings -->
                <GroupBox Header="Connection">
                    <StackPanel>
                        <!-- Hostname -->
                        <TextBlock Text="Hostname / IP Address:" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Hostname, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

                        <!-- Port -->
                        <TextBlock Text="Port:" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}"
                                 Width="100" HorizontalAlignment="Left" Margin="0,0,0,12"/>

                        <!-- Auto-connect -->
                        <CheckBox Content="Automatically connect when WinTAK starts"
                                  IsChecked="{Binding AutoConnect}"
                                  Margin="0,0,0,12"/>

                        <!-- Reconnect Interval -->
                        <TextBlock Text="Reconnect interval (seconds):" Margin="0,0,0,4"/>
                        <StackPanel Orientation="Horizontal">
                            <Slider Value="{Binding ReconnectIntervalSeconds}"
                                    Minimum="5" Maximum="60"
                                    Width="200"
                                    VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ReconnectIntervalSeconds}"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       MinWidth="30"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <!-- Advanced Settings (expandable for future) -->
                <GroupBox Header="Advanced">
                    <StackPanel>
                        <!-- Stale Node Timeout -->
                        <TextBlock Text="Stale node timeout (hours):" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding StaleNodeTimeoutHours, UpdateSourceTrigger=PropertyChanged}"
                                 Width="60" HorizontalAlignment="Left" Margin="0,0,0,12"/>

                        <!-- Topology Overlay -->
                        <CheckBox Content="Show topology overlay on map"
                                  IsChecked="{Binding TopologyOverlayEnabled}"
                                  Margin="0,0,0,8"/>
                    </StackPanel>
                </GroupBox>

                <!-- Placeholder for future settings -->
                <!--
                <GroupBox Header="Channel Settings">
                </GroupBox>

                <GroupBox Header="Display Settings">
                </GroupBox>
                -->
            </StackPanel>
        </ScrollViewer>

        <!-- Save/Cancel Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Click="OnCancel" Margin="0,0,12,0"/>
            <Button Content="Save" Click="OnSave" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
